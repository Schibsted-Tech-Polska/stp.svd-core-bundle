{% set _title = 'error.404.title'|trans({'%status_code%': status_code, '%status_text%': status_text }) %}
{{ _title|striptags|raw }}
{% for key in 1..(_title|striptags|raw|length) %}={% endfor %}


{{ 'error.404.subtitle'|trans|striptags|raw }}

{{ 'error.404.description'|trans|striptags|raw }}
